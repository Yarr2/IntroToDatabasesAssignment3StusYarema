# 1.Solution explanation 

В функції calculate_order_total, я фільтрую всі замовлення по наданому p_order_id, далі рахую суму, функція coalesce треба щоб в випадку пустого замовлення функція повернула 0 а не NULL.

в обох процедурах я спочатку перевіряю вхідні дані на помилки після чого виконую мету процедури. В if на рядку 101-104, я вибираю продукт та повертаю його кількість на складі, або 0 якщо такого не існує.

в першому тригері, if треба бо в випадку tg_op = 'DELETE', new = null, що не дає нам перерахувати total.  

# 2.ExpAnalyze explanation
Спочатку Postgre виконує Join, для цього вона за допомогою Sequence scan перевіряє умову join.
далі воно з'єднує самі таблички, після чого знову робить Sequence scan для того щоб відфільтрувати замовлення з id = 1. Postgre використав Hash Join i Sequence scan, як видно з рядків 3, 6, 11.
# 3.Git screenshoot
